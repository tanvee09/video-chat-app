<!DOCTYPE html>

<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profile</title>
  
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>  
  <script src='https://kit.fontawesome.com/a076d05399.js' crossorigin='anonymous'></script>
  <link rel="stylesheet" href="/css/login_register.css">
  <link rel="stylesheet" href="/css/navbar.css">

</head>

<body>

  <nav class="navbar fixed-top navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="/"><img src="/resources/logo.png" style="height: 40px; margin-right: 5px;"> Meets</img></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item" style="margin-right: 5px;">
              <a class="nav-link" href="/profile">Profile</a>
            </li>
            <li class="nav-item" style="margin-right: 5px;">
              <a class="nav-link" href="/logout">Sign Out</a>
            </li>
          </ul>
        </div>
    </div>
  </nav>

  <% if(typeof errors != 'undefined') { %>
    <% errors.forEach(error => { %>
      <div class="alert alert-danger alert-dismissible fade show profile-alert" role="alert">
        <%= error.message %>
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
    <% }) %>
  <% } %>

  <% if(messages.error) { %>
    <div class="alert alert-danger alert-dismissible fade show profile-alert" role="alert">
      <%= messages.error %>
      <button type="button" class="close" data-dismiss="alert" aria-label="Close">
        <span aria-hidden="true">&times;</span>
      </button>
    </div>
  <% } %>

  <div class="outside-container profile-container" id="container">
    <div class="form-container">
      <form action="/profile" method="POST">
        <% if (messages.success_msg) { %>  
          <p class="success_msg"><%= messages.success_msg %></p>
        <% } %>
        <i class='fas fa-user-circle'></i>
        <h1>Profile</h1>
        <input name="name" id="name" type="text" placeholder="Full Name" value="<%= name %>" />
        <input name="email" id="email" type="email" placeholder="Email" value="<%= email %>" />
        <input name="new_password" id="new_password" type="password" placeholder="New Password (Leave empty if you don't want to change)" value="">
        <input name="password" id="password" type="password" placeholder="Old Password" />
        <button class="form-button">Save Profile</button>
      </form>
    </div>
  </div> 

  </body>
</html>